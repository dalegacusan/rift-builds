export const { MONGODB_URI, TEST_MONGODB_URI } = process.env;
export const { RECAPTCHA_SECRET_KEY } = process.env;
export const PORT = process.env.PORT || 8080;
export const NODE_ENV = process.env.NODE_ENV;
